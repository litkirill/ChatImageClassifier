"""Initialization module for the config package.

This package consolidates configurations and logging setups used across the application.
It imports and makes available the application configuration settings and the configured logger.
"""

from .config import *
from .logger_setup import logger
